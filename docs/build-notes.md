# Build Notes — Honda Warranty & Repair-Order Triage
Log of what was actually built, in order, with verification status.

## Jul 13 — EDA & schema artifacts
- `eda/01_eda_schema_discovery.ipynb` (35 cells) — corpus interrogation, zero LLM calls: structural profiling (nulls/dupes/ranges → structured fields trustworthy ⇒ pass-through), text register profiling (shorthand density: NFF/OOW/R&R/DTC/TSB/CEL…), subsystem evidence, warranty-language mapping, severity/flag evidence. **Executed end-to-end ✔**
- `eda/extraction_schema.json` — JSON Schema draft 2020-12, `RepairNoteExtraction`, additionalProperties:false, 10 extracted fields. Authored from notebook-01 evidence.
- `eda/schema_spec.yaml` — human-readable spec with per-field provenance (which notebook step justifies it) + pass-through identity block.
- `eda/02_schema_validation.ipynb` — validation instrument (V1–V5), writes `schema_validation_report.json`. **Executed end-to-end ✔ — all checks pass:** 100% subsystem coverage; warranty signals 6 covered / 5 denied / 11 undetermined / 8 silent; all flags trigger; schema↔lexicon consistent.
- Known lexicon limitation (kept deliberately, documented in notebook): substring matching required masking denied-spans before scanning covered-patterns ("not warranty." vs "warranty.") — evidence for why the extractor is an LLM, not rules.

## Environment notes
- Notebooks expect `repair_notes_sample.csv` + `extraction_schema.json` resolvable from notebook dir (looks in ., .., ./schema, ../schema, ./data).
- Deps for notebooks: pandas, (optional) spacy + en_core_web_sm. No LLM/API deps.

## Next build steps
- [ ] Backend skeleton: FastAPI + boto3 (env-configured endpoint) + OpenRouter client (env-configured)
- [ ] Extraction service: cache-check → LLM call → schema validation (jsonschema/pydantic) → S3 write
- [ ] Aggregation endpoint + dashboard
- [ ] docker-compose (frontend, backend, LocalStack), .env.example
- [ ] README, LLM-USAGE.md, docs-alongside-code
- [ ] SDD item: `schema_tools` module — validation flag (default ON, gates pipeline, exit code) + discovery flag (default OFF, generates JSON+YAML from one internal definition); pipeline must load schema at runtime (schema-driven extractor)
- Bugfix (user-found, local run): abbreviation-mining cell had `re.fullmatch(r'(LF|RF|LH|RH)?', '')` — always matches empty string, so the `not` guard was always False and cap_tokens stayed empty (also silently emptied the downstream unknown-token mining). Replaced with simple `tok not in {'A','I'}` exclusion. Verified: mines NFF/AC/R&R/TSB/CEL/OOW...; unknown-token review queue now correctly surfaces ['AGAIN','AC','NOT'].
- [ ] STRETCH (deferred, do only if core loop done): Part 3 embed+cluster cell — sentence-transformers/all-MiniLM-L6-v2 (~90MB) + clustering so generated subsystem enum comes from semantic clusters, demoing the real no-LLM stage of budgeted-EDA generation. Guarded cell (Claude sandbox has no HF network access; Sunit validates locally). Prefer model pull at Docker build (HF_HOME env) over packaging weights in zip.
- Bugfix (user-found): pat_repeat was overfit ('3rd visit' misses 2nd/4th; 'jump started twice' fitted to RO-100013) AND conflated intermittent (fault behavior) with repeat_visit (service history) — RO-100015 is intermittent but has no prior-visit evidence. Fixed: generalized ordinal regex \b\d+(st|nd|rd|th)\s+visit + separate pat_intermittent; FLAG_LEXICON de-overfitted + intermittent_signal added. Precision limits documented in-cell (bare 'twice' = within-visit frequency; 'advised return if reoccurs' = future instruction) — regex stays a validation instrument, LLM extractor owns the per-note decision.
- RESOLVED (Jul 14, Session 2): `intermittent` added as sixth severity_flag. Corpus-evidenced (6 notes; overlaps 4/5 NFF notes = the "watch list" combo).
- Added V6 hybrid cell (guarded — sandbox has no HF access; Sunit validates locally with sentence-transformers + MiniLM): 6a subsystem regex vs semantic-top1 agreement, 6b resolution disagreements + regex-silent notes, 6c semantic coverage floor. schema_spec.yaml enriched with semantic_descriptions per enum value (incl. intermittent, pending flag decision).
- Added Part 3b: extend_schema() (monotonic extension w/ assertions) + jsonschema backward-compat proof, demoed with `intermittent` as optional severity_flag addition. Executed, proof passes.
- Reconciled stale Part 1 step 9 (user-found): pre-V6 zero-shot NLI + generic NER cells replaced with design record — zero-shot rejected (OOD shorthand, uncalibrated, 1.6GB) in favor of V6 MiniLM + semantic_descriptions; generic NER rejected (person/org/location labels, no vehicle parts); drift-detector framing re-based on V6 disagreement rate. Wrap-up cell updated.

## Jul 14 — Session 2: repo seeded, schema v0.2.0
- Consumed the handoff (`handoff-repair-note-triage`) into the `warranty-repair-triage` repo: `schema/` (contract + spec), `eda/` (notebook + generated_* + validation report), `data/repair_notes_sample.csv`, `docs/` (four running logs), root `CLAUDE.md`.
- Schema bumped **v0.1.0 → v0.2.0**: `intermittent` added to `severity_flags`. Written into both `schema/extraction_schema.json` (`$comment` version marker) and `schema/schema_spec.yaml` (`changelog` block, `intermittent` added to `fields`).
- Required-vs-optional call: made `intermittent` **required** in the v0.2.0 authored contract. Rationale — this is a fresh baseline with no cached S3 records to invalidate, and symmetry with the other 5 flags forces the LLM to judge it on every note (complete records). The monotonic-OPTIONAL rule (decision #8) governs *production discovery* extending against a live cache, a different context. **Sunit: flag for override if you'd rather ship it optional to keep the Part 3b demo's optional-addition story literally identical.**
- Verified: `schema/extraction_schema.json` parses; `severity_flags.required` and `.properties` both carry all 6 flags incl. `intermittent`.
- Note: `eda/` artifacts (notebook, `generated_*`, `schema_validation_report.json`) are the v0.1.0-era EDA record and were kept as-is; `schema_validation_report.json` v3 already counts `intermittent_signal: 6`, consistent with the promotion.
- Drafted `docs/sdd.md` (components, interfaces, env contract, S3 layout, aggregate strategy, prompt design, dashboard/review-queue UX, ~2-day build order, real-AWS mapping). Four design questions (A–D) parked for Sunit's ratification in §13.
- **Handoff DISCONNECTED:** the external `handoff-repair-note-triage` folder is no longer a source; the repo is authoritative. Verified no in-repo code/path references it (mentions are historical prose only).
- Drafted `LLM-USAGE.md` from docs + CLAUDE.md — covers both AI-assisted development (with the concrete human-caught bugs as the integrity signal) and runtime product LLM usage. Marked to finalize at build.
- Deferred: one polished interview-specific project doc to be synthesized from these running logs AFTER the build completes (these `docs/*-notes.md` stay as raw logs).
- `LLM-USAGE.md` scope pinned from the assignment brief: **dev-time AI disclosure only** (tools / what-for / significant changes & why), brief + honest + first-person. Rewrote it to that pattern; dropped the runtime "product LLM usage" section (that content belongs in README/architecture). Only Claude Code listed (the only tool actually used so far).
- SDD §4 extended (Topic B provenance): S3 record `meta` now stamps `prompt_version` + `git_sha` alongside `schema_version`/`note_sha256`. Guard: provenance fields are metadata-only, **excluded from the cache-hit check**, so a rebuild at the same schema+prompt doesn't invalidate the cache. Two impl details deferred to build: prompt-hash scheme (`extract.py`) and git_sha injection method (Dockerfile), both env-injected.
- **Decisions E & F written into SDD** (§7.1, §7.2). E = aggregation-as-one-grammar (query object → DuckDB parameterized SQL; 3 surfaces = presets/query-panel/visuals; tunable score; LLM-only-at-ingest; no MCP in core; fixed-schema scaling invariant). F = provider ladder + `LLM_PROVIDER` seam (OpenRouter→Bedrock→vLLM). Reconciled B/D (refined-by-E notes), component map (`providers/`, `query_engine.py`), env contract, API (`POST /aggregate`, `GET /presets`), open items.
- **Decision G written into SDD** (§7.3): async ingest / live-progress pipeline (SQS + in-process worker fallback · S3 job manifest · polling · poll-while-active). Wired §9 (`POST /ingest`, `GET /jobs/{id}`), §2 (`ingest.py`/`worker.py`), env, build order, §12 AWS mapping.
- **ADR set created** (`docs/decisions/`): index+template README + ADR-0001 (adopt ADRs + self-composing docs) + ADR-0002…0008 (Decisions A–G). DRY: ADRs = canonical "why", SDD = "how", CLAUDE.md = locked index.
- **Self-composing documentation system** (`docs/README.md`): human-readable doc map (layers × reader × change-rate) + a **load-bearing LLM compose instruction** + a machine-readable manifest (compose-order/authority). Point any LLM at `docs/` → it composes the full human-readable narrative; supports reader focus directives. This is the entry point for understanding/regenerating the whole doc picture. Captured for the final project doc in `project-doc-outline.md §A.1`.
- **Build-time practice set (Sunit):** per-feature **primers** in `primers/` (separate from docs/) — concept-first, skimmable, cite code, "Probe deeper?" hooks so Sunit chooses where to get granular eyes. Written as each feature lands; Tier-1 selection + template in `primers/README.md`. Focus on assignment-critical parts (AI depth/cloud/judgment).
- **Final ratifications (Session 2) — architecture now CLOSED:** C ratified as written; **priority-score default = severity-dominant** (single critical/high can outrank a larger cluster; count+recency secondary; boosts) with the **full weight config visible+tunable+reset in the UI = core**; **frontend = plain React (Vite) + Nivo + CSS tables** (Streamlit rejected — brief pins Next/React + separate containers). SDD status flipped to RATIFIED (all A–G). Next: build phase.
